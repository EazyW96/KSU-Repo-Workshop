using UnityEngine;

public class HelloBadge : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello GitHub Badge from Unity!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
